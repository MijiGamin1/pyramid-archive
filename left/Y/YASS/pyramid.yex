$l = input()
$b = ""
$n = 0
$t = 1
for ($i = 0; $i < count($l); $i++)
    $n = $n + (character_to_integer($l[count($l)-$i-1])-48) * $t)
    $t = $t * 10
end for
for ($j = 0; $j < $n; $j++)
    $b = $b & "*"
    print($b)
end for
